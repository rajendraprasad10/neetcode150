def reserce_list(lst):
    """
    Reverses the given list.

    Args:
        lst (list): The list to be reversed.

    Returns:
        list: The reversed list.
    """
    return lst[::-1]

print(reserce_list([1, 2, 3, 4, 5]))  # Output: [5, 4, 3, 2, 1]